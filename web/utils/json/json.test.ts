describe('parseJSONResponse', () => {
  // TODO: find a way to mock the global Response and fetch
  // currently they are undefined
  it('throws an error of type ParsingJSONError if provided with faulty body', () => {});
});
